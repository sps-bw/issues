<!DOCTYPE html>
<html>
<head>
    <title>Index</title>
    <meta name="viewport" content="height=185"/>
    <link rel="stylesheet" type="text/css" href="css/index.css">
    <meta charset="UTF-8">
    <script src="gfx/jquery.js"></script>

    <script>

        var width = 3000;

        $(function() {

          width = 0;

          $( "div.section" ).each(function( index ) {
            width = width + $( this ).width();
          });

          width = width + 200;

          $('body').css('width', width + 'px');

        });

    </script>

</head>
<body>
  <div id="index">
    <div class="navigation">
      <ol>
          <div class="section">
            <% @articles.each do |article| %>
              <% if article['banner'] %>
                <a href="<%= article['path'] %>" style="background-image: url(images/<%= article['banner'] %>)" class="banner"><span class="text"><%= article['title'] %></span></a>
              <% else %>
                <a href="<%= article['path'] %>"><span class="text"><%= article['title'] %></span></a>
              <% end %>
            <% end %>
          </div>
      </ol>
    </div>
  </div>
</body>
